# Fizzy.ImageViewer 开发约定

先读 [README](README.md) 和相关组件文档；验证方式见 [验证指南](docs/validation.md)。联合工作区还遵循父目录约定。

## 文件组织

- 源码和测试按功能归属组织，具体目录与例外遵循 [架构文档](docs/architecture.md#placement-rules)。
- 公共顶层类型使用同名文件；内部独立类型同样处理，宿主专用嵌套类型和紧密关联的内部查询类型族保留聚合。
- 新文件优先进入现有功能目录；目录调整须有职责归属依据，不因个人风格反复移动文件或改变公共命名空间。

## 文档质量

- 先明确读者、使用场景、核心问题和预期行动，再确定主旨与结构。
- 按读者的理解或操作顺序组织内容，说明用途，展开必要概念、步骤与例外，并给出结论或下一步。
- 只呈现最终结论、必要依据和行动要求；除非文档目的就是记录历史，否则不写讨论、生成或修改过程。
- 每一节都应服务于全文目标；合并重复内容，删除无关细节，确保读者无需了解生成背景也能理解和使用。
- 复杂文档依次进行结构审查、内容整理、语言润色和最终复查；未经明确要求，不同时大幅改变结构、范围和语气。
- 完成后用一句话检验全文主旨，并检查重复、跳跃、过度解释和过程性叙述。

## 设计原则

- API 保持语义清晰、职责单一、行为一致，兼顾当前需求与有依据的扩展方向。
- 优先复用现有抽象与组合方式；必要时改进通用机制，避免为单个场景叠加特殊接口、旁路逻辑或硬编码分支。
- 以较低复杂度统一处理现有场景，减少概念、耦合和调用方需要理解的细节。
- 共享行为统一表达，特有能力归具体类型；组合约束可在边界验证，不为少量非法组合拆出多套近似类型。
- 扩展点应对应明确需求与实际变化点，避免为假想需求引入过度抽象或复杂配置。
- 不以旧代码兼容性为约束，允许破坏性重构；移除过时接口与兼容逻辑，同步更新调用方、测试和文档，避免遗留旧实现或过渡方案。

## Commit 信息

- 使用 `type(scope): description` 格式，scope 可省略；type、scope 及英文描述均以小写开头，破坏性变更用 `!` 标记。
- 示例：`refactor(viewer): simplify frame ownership`、`chore: update dependencies`、`refactor(api)!: remove legacy interface`。
