<Project>
  <PropertyGroup>
    <!-- Package versions are declared by each project so this directory builds independently. -->
    <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
  </PropertyGroup>
</Project>
